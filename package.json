{
  "name": "onecli-plugin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsup --config hooks/tsup.config.ts",
    "typecheck": "tsc -p hooks/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
